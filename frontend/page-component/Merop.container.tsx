import {MeropContainerProps} from "@/page-component/Merop.container.props";
import {Htag} from "@/components/Htag/Htag";

export const MeropContainer = (MeropCards :MeropContainerProps) => {
  return <div>
    <Htag type={'h1'}>Мероприятия</Htag>
  </div>
}
